## Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
## SPDX-License-Identifier: Apache-2.0

# End-of-pipeline teardown of Application Signals from a shared EKS test cluster.
#
# This restores the original (pre-parallelization) cleanup behavior — tear down App Signals after the
# tests — but in a way that is safe for the parallel model. clean-app-signals.sh deletes the shared
# amazon-cloudwatch-observability addon and the cloudwatch-agent IRSA service account; doing that
# while EKS test jobs are running breaks them (agent loses IRSA -> AccessDenied -> dropped telemetry).
#
# The caller invokes this LAST, gated on every EKS job (all eks-py* and service-events-eks), so it
# runs only when nothing else is using the cluster — the same exclusivity that made enable/clean safe
# in the old sequential design. The next pipeline run's first EKS job reinstalls App Signals via
# enable-app-signals.sh, exactly as before.
name: EKS Cleanup Application Signals

on:
  workflow_call:
    inputs:
      test-cluster-name:
        required: true
        type: string
      aws-region:
        required: false
        type: string
        default: 'us-east-1'

permissions:
  id-token: write
  contents: read

env:
  CLUSTER_NAME: ${{ inputs.test-cluster-name }}
  E2E_TEST_AWS_REGION: ${{ inputs.aws-region }}
  E2E_TEST_ACCOUNT_ID: ${{ secrets.APPLICATION_SIGNALS_E2E_TEST_ACCOUNT_ID }}
  E2E_TEST_ROLE_NAME: ${{ secrets.APPLICATION_SIGNALS_E2E_TEST_ROLE_NAME }}

jobs:
  eks-cleanup-app-signals:
    # always() so cleanup runs even if an upstream test job failed — but it is gated (via the caller's
    # needs:) on all EKS jobs finishing, so it is still exclusive.
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@a03048d87541d1d9fcf2ecf528a4a65ba9bd7838 # v5.0.0
        with:
          role-to-assume: arn:aws:iam::${{ env.E2E_TEST_ACCOUNT_ID }}:role/${{ env.E2E_TEST_ROLE_NAME }}
          aws-region: ${{ env.E2E_TEST_AWS_REGION }}

      - name: Download cleanup script
        run: |
          mkdir -p enablement-script && cd enablement-script
          wget -q https://raw.githubusercontent.com/aws-observability/application-signals-demo/main/scripts/eks/appsignals/clean-app-signals.sh
          chmod +x clean-app-signals.sh

      - name: Set up kubeconfig
        run: aws eks update-kubeconfig --name ${{ env.CLUSTER_NAME }} --region ${{ env.E2E_TEST_AWS_REGION }}

      # clean-app-signals.sh calls `eksctl delete iamserviceaccount` to remove the cloudwatch-agent
      # IRSA service account, so eksctl must be on PATH.
      - name: Install eksctl
        run: |
          curl -sLO "https://github.com/weaveworks/eksctl/releases/latest/download/eksctl_Linux_amd64.tar.gz"
          tar -xzf eksctl_Linux_amd64.tar.gz -C /usr/local/bin
          rm -f eksctl_Linux_amd64.tar.gz
          eksctl version

      # Reap every leftover sample-app namespace, then tear down shared state unconditionally.
      #
      # This job is gated `needs: [every EKS job] + if: always()`, so it runs last in its pipeline even
      # when jobs fail. Both entry points into this cluster (main-build and the E2E caller) declare
      # `concurrency:` with cancel-in-progress: false, so a second pipeline QUEUES rather than
      # overlapping -- there is no concurrent run to protect when we get here.
      #
      # The previous version skipped teardown (exit 0) whenever any ns-* namespace existed. A job that
      # died before its own terraform destroy / namespace delete (e.g. a `kubectl wait` timeout) left an
      # orphan namespace behind, which then tripped that guard and silently skipped teardown while
      # REPORTING SUCCESS. The cluster stayed warm, so the next pipeline never cold-started and the
      # leaked namespace persisted indefinitely. Leaking state is worse than reaping it here.
      - name: Reap leftover test namespaces
        timeout-minutes: 15
        run: |
          stale=$(kubectl get namespace -o name 2>/dev/null | grep -E 'namespace/ns-' | sed 's#namespace/##' || true)
          if [ -z "$stale" ]; then
            echo "No leftover test namespaces."
            exit 0
          fi
          echo "Reaping leftover test namespaces from this pipeline:"
          echo "$stale"
          # Best-effort: a namespace wedged in Terminating must not block shared teardown below.
          for ns in $stale; do
            kubectl delete namespace "$ns" --ignore-not-found --timeout=180s || \
              echo "::warning::Namespace $ns did not delete cleanly; continuing."
          done
          echo "Remaining namespaces (if any):"
          kubectl get namespace -o name 2>/dev/null | grep -E 'namespace/ns-' || echo "  none"

      - name: Clean up Application Signals
        working-directory: enablement-script
        run: |
          # `yes |` answers any interactive prompt in the script non-interactively.
          yes | ./clean-app-signals.sh \
            ${{ env.CLUSTER_NAME }} \
            ${{ env.E2E_TEST_AWS_REGION }} \
            default || echo "::warning::clean-app-signals.sh exited non-zero; verifying actual state below."

      # Belt-and-braces: clean-app-signals.sh is best-effort, so assert the cluster is genuinely cold
      # and sweep anything it missed. Without this, a partial teardown leaves the next pipeline warm --
      # which is exactly how the IRSA race stayed hidden (a warm cluster never re-runs cold-start setup).
      - name: Verify cluster is cold and sweep remnants
        timeout-minutes: 15
        run: |
          # 1) Addon must be gone.
          if aws eks describe-addon --cluster-name "${{ env.CLUSTER_NAME }}" \
               --addon-name amazon-cloudwatch-observability \
               --region "${{ env.E2E_TEST_AWS_REGION }}" >/dev/null 2>&1; then
            echo "Addon still present; deleting explicitly."
            aws eks delete-addon --cluster-name "${{ env.CLUSTER_NAME }}" \
              --addon-name amazon-cloudwatch-observability \
              --region "${{ env.E2E_TEST_AWS_REGION }}" >/dev/null 2>&1 || true
            for i in $(seq 1 30); do
              aws eks describe-addon --cluster-name "${{ env.CLUSTER_NAME }}" \
                --addon-name amazon-cloudwatch-observability \
                --region "${{ env.E2E_TEST_AWS_REGION }}" >/dev/null 2>&1 || break
              echo "  waiting for addon deletion ($i)"; sleep 10
            done
          fi

          # 2) cloudwatch-agent IRSA service account must be gone, along with its eksctl CFN stack.
          #    A surviving stack makes the next `eksctl create iamserviceaccount` behave unpredictably.
          if kubectl get sa cloudwatch-agent -n amazon-cloudwatch >/dev/null 2>&1; then
            echo "cloudwatch-agent SA still present; deleting via eksctl."
            eksctl delete iamserviceaccount --name cloudwatch-agent --namespace amazon-cloudwatch \
              --cluster "${{ env.CLUSTER_NAME }}" --region "${{ env.E2E_TEST_AWS_REGION }}" || \
              kubectl delete sa cloudwatch-agent -n amazon-cloudwatch --ignore-not-found || true
          fi

          # 3) Sweep orphaned eksctl IRSA CloudFormation stacks. `eksctl delete iamserviceaccount`
          #    normally removes both the SA and its stack, but if the SA was deleted by other means the
          #    stack survives in CREATE_COMPLETE. Stale stacks accumulate run over run and can make the
          #    next `eksctl create iamserviceaccount` behave unpredictably.
          orphans=$(aws cloudformation describe-stacks --region "${{ env.E2E_TEST_AWS_REGION }}" \
            --query "Stacks[?contains(StackName, 'eksctl-${{ env.CLUSTER_NAME }}-addon-iamserviceaccount-amazon-cloudwatch')].StackName" \
            --output text 2>/dev/null || true)
          if [ -n "$orphans" ] && [ "$orphans" != "None" ]; then
            for st in $orphans; do
              echo "Deleting orphaned IRSA stack: $st"
              aws cloudformation delete-stack --region "${{ env.E2E_TEST_AWS_REGION }}" --stack-name "$st" || \
                echo "::warning::Could not delete stack $st"
            done
          else
            echo "No orphaned cloudwatch-agent IRSA stacks."
          fi

          # 4) Report final state. Namespaces wedged in Terminating are surfaced but not fatal --
          #    failing here would mask the pipeline's real test results.
          echo "=== Final cluster state ==="
          echo "addons:     $(aws eks list-addons --cluster-name ${{ env.CLUSTER_NAME }} --region ${{ env.E2E_TEST_AWS_REGION }} --query 'addons' --output text 2>/dev/null || echo '<none>')"
          echo "cw SAs:     $(kubectl get sa -n amazon-cloudwatch --no-headers 2>/dev/null | awk '{print $1}' | tr '\n' ' ' || echo '<none>')"
          leftover=$(kubectl get namespace -o name 2>/dev/null | grep -E 'namespace/ns-' || true)
          if [ -n "$leftover" ]; then
            echo "::warning::Test namespaces still present after cleanup (likely stuck Terminating):"
            echo "$leftover"
          else
            echo "test ns:    <none> — cluster is cold"
          fi
